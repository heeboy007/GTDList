import { useState } from "react";

function ListView(props) {
    let tasks = props.tasks;
    console.log(tasks);
    return (
        <div>
            
        </div>
    );
}

export default ListView;